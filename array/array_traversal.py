# created by shavendra 'badshasha'

# travers mean visiting each and every element of the array
# big o == O(n)
from array import *

array_name = array('i',[1,2,3,4])

def array_traversal(array):
    for i in array:
        print(i)

array_traversal(array_name)
